# Dependências do app novo (agente_medico/). NÃO é o requirements.txt da raiz,
# que pertence ao legado Streamlit (app.py, modules/) — D-ARQ-75 cláusula 3.
#
# Conjunto medido por varredura AST de agente_medico/{motor,superficie,adaptadores}
# (003.ES; a varredura por grep de D-ARQ-75 era cega a import indentado).
# Guardado por agente_medico/tests/test_requirements_app.py — não editar à mão
# sem rodar esse teste.

# Piso 1.42.0 = versão que introduz st.login (fatia 2 de 003.ES).
# Fonte: docs.streamlit.io/develop/quick-reference/release-notes/2025
streamlit>=1.42.0,<2.0.0
pdfplumber>=0.10.0
PyYAML>=6.0
python-docx>=1.1.0
requests>=2.31.0
